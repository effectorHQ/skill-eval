# Changelog

## [Unreleased] — 2026-03-15 (Phase D)

### Added
- `.gitignore` for Node.js projects
- `homepage` and `bugs.url` fields in `package.json`

### Changed
- Updated keywords: removed `openclaw`, `clawhub`; added `effectorhq`
- Standardized `engines.node` to `>=18`
- Repository URL now includes `.git` suffix
